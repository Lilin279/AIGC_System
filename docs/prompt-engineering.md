# 提示词工程记录

## P1 结构化抽取约束

- 系统提示明确要求只输出合法 JSON，配合 `response_format={"type":"json_object"}`。
- 用户提示限制实体类型与三类关系，要求 `source/target` 必须引用知识点名称。
- 每个知识点包含来源片段，服务端再使用 Pydantic 校验；网络故障、429、5xx 等最多尝试两次。截断属于长度限制错误，不使用相同请求盲目重试，并保留响应中的 Token 用量记录。
- 真实问答提示要求只依据给定课件分片和图谱邻居证据，证据不足时明确拒答。

默认模型为 `deepseek-v4-flash`，可通过 `DEEPSEEK_MODEL` 覆盖。官方 JSON Output 要求提示中出现 JSON 并提供目标结构示例，当前实现已经满足。

真实抽取正文上限为 60,000 字符，输出预算为 12,000 tokens。首次结果少于 20 个知识点或未覆盖 `contains / prerequisite / related` 时，系统执行一次增量补全调用，只返回缺失节点和关系。服务端合并并去重，保留首次节点的定义和来源。

补全截断、超时或结构不合法时，保留首次有效结果为待审核候选，并在任务消息及版本摘要中显示“自动补全未完成”和质量提醒。首次调用本身没有有效结果时仍报告失败，不会把截断的 JSON 或空图谱当作成功结果。

## 知识点抽取提示词

```text
你是高校课程知识图谱抽取专家。只输出合法 JSON，不要输出 Markdown。
从课程原文抽取 20 个有教学价值的知识点，类型限定为
chapter/concept/skill/formula/example/project。每个节点必须给出
document_id、page_no、原文 excerpt 和 confidence，filename 由服务端恢复。
excerpt 必须直接来自原文，重复概念按标准名称合并。
定义最多50字，excerpt 最多40字，每项保留一个直接来源，关系最多30条。
```

## 关系识别提示词

```text
请基于课程原文识别关系，关系类型只能从 contains、prerequisite、related 中选择，
且三类必须覆盖。source/target 必须引用已输出知识点名称；每条关系也要给出来源、
页码、原文片段、置信度和判断理由。
```

## RAG 问答提示词

```text
你是课程助教。只能依据编号证据回答；证据不足时明确说不知道。
引用必须使用 [1][2] 格式，不得引用未提供的材料，也不得用模型记忆补充课程外事实。
```

## 练习生成提示词

```text
只依据编号课程证据，为薄弱知识点生成基础题、应用题和易错题各一道。
输出 JSON，包含题型、难度、题目、答案、解析和 evidence_ids。
```

## 迭代记录

| 日期 | 模板 | 问题 | 调整 | 结果 |
| --- | --- | --- | --- | --- |
| 2026-08-24 | 初版 | 原型使用 Mock 流程 | 固定 JSON 输出字段 | 可演示 |
| 2026-08-28 | 来源版 | 输出无法定位原课件 | 增加文档 ID、页码、原文、置信度与理由 | 可追溯 |
| 2026-08-28 | 质量版 | 小课件可能不足 20 节点 | 一次补全调用并保留质量警告 | 待真实 Key 验收 |
| 2026-08-28 | GraphRAG 版 | 通用回答可能使用课外知识 | 只允许编号证据并要求不足时拒答 | HTTP Mock 已通过 |
| 2026-09-13 | 增量补全与截断修复 | 补全输出截断导致首次成功结果被丢弃 | 输出预算6000→12000；压缩重复字段；只补缺失内容；补全失败保留候选及警告 | 后端26项测试通过；原出错课程真实调用返回21节点、21关系、三类关系，无截断提醒 |
