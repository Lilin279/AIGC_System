# CourseGraph AI 设计规范

> 版本：1.0
> 适用范围：学生端、教师端、管理员端、登录注册页及后续新增页面。
> 执行要求：新增或修改页面前必须先阅读本文件；颜色、排版、间距和组件应优先使用本规范中的设计令牌，禁止在页面中随意增加近似色值或另一套组件风格。

## 1. 设计方向

整体采用克制的 Aurora SaaS 风格：冷白工作区、轻量半透明表面、紫色主操作、青绿成功反馈，以及清晰的数据密度。视觉参考中的大面积紫色仅用于登录品牌区和关键强调，业务页面保持明亮、安静、便于长时间阅读。

核心原则：

- 信息层级优先，装饰不能干扰图谱、表格和表单。
- 页面使用统一的 4px 间距体系，不以空白卡片堆叠内容。
- 同一屏幕只保留一个主要操作色，状态色只表达状态。
- 阴影用于区分层级，不用于制造悬浮装饰。
- 图谱渲染逻辑独立，外层容器遵循本规范，图内部交互参数不因视觉改版而修改。

## 2. 配色

### 2.1 品牌色

| 令牌 | 色值 | 用途 |
|---|---|---|
| `--primary` | `#6D5EF0` | 主按钮、当前导航、焦点、关键数据 |
| `--primary-deep` | `#4A3ED6` | 主按钮悬停、强调文字 |
| `--primary-soft` | `#EFEDFF` | 选中态、浅色提示背景 |
| `--accent-cyan` | `#3FB8E8` | AI、图谱与信息类辅助强调 |
| `--accent-rose` | `#F87BA6` | 少量提醒与品牌辅助 |
| `--accent-amber` | `#F5B26B` | 警告和待处理辅助 |

主色不使用全页大面积渐变。允许在品牌标记、进度条等小面积元素中使用 `#7C6CF0` 到 `#3FB8E8` 的渐变。

### 2.2 中性色

| 令牌 | 色值 | 用途 |
|---|---|---|
| `--ink` | `#23263B` | 标题、核心数据、主要文字 |
| `--ink-soft` | `#5C617F` | 正文、表格内容、说明文字 |
| `--ink-faint` | `#9298B0` | 占位符、时间、弱提示 |
| `--canvas` | `#F2F4FB` | 应用背景 |
| `--surface` | `rgba(255,255,255,.88)` | 卡片和工具栏 |
| `--surface-solid` | `#FFFFFF` | 输入框、弹窗、强内容表面 |
| `--border` | `rgba(72,76,108,.13)` | 默认边框和分割线 |
| `--border-strong` | `rgba(72,76,108,.22)` | 输入框和强调边框 |
| `--nav-bg` | `rgba(250,250,255,.88)` | 左侧导航背景 |

### 2.3 状态色

| 状态 | 主色 | 浅背景 | 文字色 |
|---|---|---|---|
| 成功/已掌握 | `#12A87B` | `#E7F8F2` | `#0B7A59` |
| 警告/待处理 | `#E8930C` | `#FFF5E5` | `#9A6207` |
| 错误/高风险 | `#E5484D` | `#FFF0F1` | `#B4232A` |
| 信息/AI | `#3B82F6` | `#EBF4FF` | `#245EA8` |

状态色不可替代主操作色。例如删除按钮使用错误色，确认应用仍使用品牌紫色。

## 3. 字体与排版

### 3.1 字体族

- 展示与数字：`Sora, Inter, Noto Sans SC, Microsoft YaHei, sans-serif`
- 正文与表单：`Noto Sans SC, PingFang SC, Microsoft YaHei, Segoe UI, sans-serif`
- 代码和班级码：`Cascadia Code, Consolas, monospace`

字体不可用时自动使用后续系统字体。字距统一为 `0`。

### 3.2 字号层级

| 层级 | 字号 | 字重 | 行高 | 使用场景 |
|---|---:|---:|---:|---|
| Display | `40px` | `800` | `1.20` | 登录品牌标题，仅限宽屏 |
| 页面标题 H1 | `28px` | `750` | `1.30` | 页面主标题 |
| 分区标题 H2 | `20px` | `700` | `1.40` | 大模块标题 |
| 卡片标题 H3 | `15px` | `700` | `1.45` | 卡片、详情面板标题 |
| 正文 | `14px` | `400` | `1.65` | 默认内容 |
| 控件文字 | `13px` | `600` | `1.40` | 按钮、导航、表格标题 |
| 辅助文字 | `12px` | `400/500` | `1.55` | 时间、状态、说明 |

移动端页面标题降为 `23px`，Display 降为 `32px`。正文不小于 `13px`。

## 4. 间距与留白

采用 4px 基础网格：

| 令牌 | 数值 | 常见用途 |
|---|---:|---|
| `--space-1` | `4px` | 图标微调、紧密文本 |
| `--space-2` | `8px` | 控件内部、按钮组 |
| `--space-3` | `12px` | 列表项、紧凑表单 |
| `--space-4` | `16px` | 卡片内部小间距 |
| `--space-5` | `24px` | 标准卡片内边距、模块间距 |
| `--space-6` | `32px` | 页面左右内边距 |
| `--space-7` | `48px` | 页面底部留白、大分区间距 |

规则：

- 桌面页面内边距：左右 `32px`，顶部 `28px`，底部 `48px`。
- 标准卡片内边距：`20px`；复杂表单卡片可用 `24px`。
- 同级模块间距：`16px`；页面标题与正文：`24px`。
- 表单标签与控件：`7px`；连续字段：`14px`。
- 移动端页面左右内边距：`16px`，模块间距保持 `12px` 以上。

## 5. 组件样式

### 5.1 按钮

- 标准高度：`40px`；紧凑按钮：`34px`。
- 圆角：`10px`，图标按钮同样为 `10px`。
- 主按钮：紫色背景 `#6D5EF0`、白字，无描边；悬停使用 `#4A3ED6`。
- 次按钮：白色或半透明白背景，`1px` 中性边框，文字使用 `#4A3ED6`。
- 危险按钮：`#E5484D` 背景、白字。
- 阴影仅用于主按钮：`0 6px 16px rgba(109,94,240,.20)`。
- 按钮文字 `13px/600`；图标推荐 `15–17px`。
- 按钮必须有 hover、focus-visible、disabled 状态。

### 5.2 输入框

- 高度不低于 `40px`，内边距 `10px 12px`。
- 背景 `#FFFFFF`，边框 `1px solid rgba(72,76,108,.18)`。
- 圆角 `10px`。
- 聚焦时边框变为主色，并出现 `0 0 0 3px rgba(109,94,240,.12)` 焦点环。
- 标签使用 `13px/600`，颜色 `#5C617F`。

### 5.3 卡片与面板

- 标准卡片圆角：`16px`；紧凑子项：`10px`；弹窗：`18px`。
- 背景：`rgba(255,255,255,.88)`；需要承载长文本时可使用纯白。
- 边框：`1px solid rgba(72,76,108,.13)`。
- 默认阴影：`0 8px 24px rgba(63,63,110,.07)`。
- 卡片之间保持 `16px` 间距，不允许卡片内再嵌套同级装饰卡片。

### 5.4 导航栏

- 桌面侧栏宽度：`248px`，固定视口高度。
- 导航背景使用近白半透明表面，与内容区通过细边框区分。
- 导航项高度：`44px`，圆角 `10px`，间距 `4px`。
- 默认文字 `#5C617F`；当前项背景 `#EFEDFF`，文字 `#4A3ED6`，字重 `700`。
- 品牌标记为 `40px` 紫色方形，圆角 `12px`。
- 顶栏高度：`76px`，使用半透明白背景和底部分割线。

### 5.5 表格

- 表头使用 `#F7F7FC`，文字 `12px/700/#5C617F`。
- 单元格使用 `13px/400/#34374D`，上下内边距 `13px`。
- 行分割线使用 `--border`；悬停行背景 `#FAFAFE`。
- 表格外部不额外套第二层卡片。

### 5.6 状态徽章与通知

- 徽章圆角为全圆角，内边距 `4px 10px`，字号 `12px/600`。
- 通知使用对应状态的浅背景与深色文字，左侧使用状态色边框或图标。
- AI 在线状态使用成功色；离线状态使用警告色。

## 6. 页面布局

- 登录页：宽屏为 `46% / 54%` 双栏，左侧品牌色、右侧表单；移动端改为上下布局。
- 业务页：左侧导航 + 顶部上下文栏 + 主内容区。
- 数据总览：指标卡独立排列，桌面 4 列，平板 2 列，手机 2 列或 1 列。
- 编辑工作台：主工作区与右侧工具栏采用约 `1fr / 380px`；窄屏改为单列。
- 列表管理：筛选和主要操作位于标题区右侧，表格占据完整内容宽度。
- 图谱页面：图谱始终是最大视觉区域，工具和详情位于右侧；不得修改图谱渲染生命周期来适配装饰效果。

## 7. 动效与可访问性

- 常规过渡时长 `160ms`，复杂布局过渡 `240ms`。
- 悬停位移最多 `2px`，避免高频业务界面产生明显跳动。
- 所有交互控件必须有 `focus-visible` 样式。
- 尊重 `prefers-reduced-motion`，关闭非必要位移动效。
- 正文与背景对比度应达到 WCAG AA；不可只使用颜色表达状态。
